# Agentic Web Assistant Icons

The extension now renders its visual identity dynamically using inline SVG data so no bitmap icon files are required. This folder remains as a placeholder should you wish to add custom packaged icons in the future.
